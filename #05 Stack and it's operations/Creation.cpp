#include<iostream>

struct stack {
    int size;
    int top;
    int * s;
}
t;

using namespace std;

int main() {
    cout << "Enter the size of the stack: ";
    cin >> t.size;
    t.s = new int[t.size];
    return 0;
    t.top =-1;
}